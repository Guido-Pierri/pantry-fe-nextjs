import {Croissant_One, Inter, Lusitana} from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] });
export const croissant = Croissant_One({subsets: ['latin'], display: 'swap', weight: '400',})
export const lusitana = Lusitana({subsets: ['latin'], display: 'swap', weight: '400',})
